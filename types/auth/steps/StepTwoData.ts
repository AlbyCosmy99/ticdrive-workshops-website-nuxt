export interface StepTwoData {
  fullAddress: {
    streetAddress: string;
    city: string;
    province: string;
    postalCode: string;
  };
  companyContact: {
    phone: string;
    email: string;
  };
  referContact: {
    phone: string;
    email: string;
  };
}
