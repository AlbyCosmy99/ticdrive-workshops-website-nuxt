export interface StepTwoData {
  fullAddress: {
    streetAddress: string;
    city: string;
    province: string;
    postalCode: string;
  };
  referContact: {
    phone: string;
    email: string;
  };
}
